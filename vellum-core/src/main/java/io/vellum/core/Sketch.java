package io.vellum.core;

import io.vellum.core.renderer.Renderer;

public class Sketch {
    private final Renderer renderEngine;

    public Sketch(Renderer renderEngine) {
        this.renderEngine = renderEngine;
    }
}